server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  include /etc/nginx/mime.types;
  charset utf-8;
  charset_types text/css application/javascript application/json text/plain application/manifest+json;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
